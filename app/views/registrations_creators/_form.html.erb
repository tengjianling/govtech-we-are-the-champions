<%= form_with(model: registrations_creator) do |form| %>
  <% if registrations_creator.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(registrations_creator.errors.count, "error") %> prohibited this registrations_creator from being saved:</h2>

      <ul>
        <% registrations_creator.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_input, style: "display: block" %>
    <%= form.text_area :user_input %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
