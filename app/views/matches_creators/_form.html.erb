<%= form_with(model: matches_creator) do |form| %>
  <% if matches_creator.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(matches_creator.errors.count, "error") %> prohibited this matches_creator from being saved:</h2>

      <ul>
        <%@matches_creator.errors.messages.values.each do |msg| %>
          <%msg.each do  |m| %>
            <li><%= m %></li>
          <%end %>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :user_input, style: "display: block" %>
    <%= form.text_area :user_input %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
